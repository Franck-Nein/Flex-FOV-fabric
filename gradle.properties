# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G -Xmx1G -XX:+UseG1GC -XX:MaxHeapFreeRatio=10 -XX:MinHeapFreeRatio=5 -XX:G1PeriodicGCInterval=30000

# Fabric Properties
	# check these on https://fabricmc.net/use
    # more helpful: https://modmuss50.me/fabric.html
	minecraft_version=1.20
	yarn_mappings=1.20+build.1
	loader_version=0.16.14

# Mod Properties
	mod_version = 1.1.8.1
	maven_group = net.id107
	archives_base_name = FlexFOV

# Dependencies
	# currently not on the main fabric site, check on the maven: https://maven.fabricmc.net/net/fabricmc/fabric-api/fabric-api
	fabric_version=0.83.0+1.20
	
